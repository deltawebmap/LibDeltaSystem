using System;
using System.Collections.Generic;
using System.Text;

namespace LibDeltaSystem.Tools
{
    /// <summary>
    /// A special form of RPC commands that utilize a privilege system that only sends parts of it to certain groups, even though it is packed as its own message
    /// </summary>
    public static class RPCPrivilegedMessageTool
    {
    }
}
